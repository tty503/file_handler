//#include <stdio.h>

//#include "file_handler_uchar.h"
/*
void create_uchar(unsigned char path[])
{
	FILE *fptr; 
	size_t size_path = sizeof(path) / sizeof(path[0]);

	char *buffer;
	for (int i = 0; i < (int)size_path; i++)
		*buffer++ = path[i];

	fptr = fopen(buffer, "w");
	fclose(fptr);
	
	La lógica de extracción la podemos verificar en una compilación normal de Linux
		y luego, cuando se este seguro del comportamiento del programa migrar a Windows. 


	Útil :  http://www.forosdelweb.com/f96/lenguaje-c-recorrer-caracteres-array-punteros-char-833865/


	
	
}*/